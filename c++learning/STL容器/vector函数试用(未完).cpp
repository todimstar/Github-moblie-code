//vector的常用函数

#include<iostream>
#include<vector>

using namespace std;


int main (){
	
	//.push_back(元素);
	//.pop.back()是直接删了弹出;
	//a[0] == a.front(); == *a.begin();
	//a[a.size()-1] == a.back() == *(--a.end());
	
	//a.empty <-----return a是否为空(bool)
	//a.clear <-----清空vector容器 
	
	return 0;
} 
